---
 - name: Install webserver and run 
   hosts:
   become: true
   tasks:
   - name: install http
     yum:
      name: httpd
      state: present
   - name: write in index.html
     command: echo "This is my first deployment with ansible tower using with git on cloud" > /var/www/html/index.html

